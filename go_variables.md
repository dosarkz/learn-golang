# Go Variables

## Variable Declaration
In Go, variables can be declared using the `var` keyword or the shorthand `:=` syntax.

### Using `var` keyword
```go
var name string
var age int

name = "Alice"
age = 30
```
